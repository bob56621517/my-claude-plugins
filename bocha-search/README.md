# bocha-search

中文搜索引擎 MCP，支持网页搜索和 AI 语义结构化卡片查询。

## 环境变量

```bash
BOCHA_API_KEY=<你的 API Key>
```

## 配置

在 Claude Code 的 `settings.json` 的 `env` 字段中设置：

```json
{
  "env": {
    "BOCHA_API_KEY": "sk-xxx"
  }
}
```
