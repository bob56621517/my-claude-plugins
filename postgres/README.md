# postgres

PostgreSQL 数据库 MCP（项目级安装），支持读写、表结构和 Schema 查询。

## 环境变量

```bash
POSTGRES_DB_URL=postgresql://user:password@host:5432/dbname
```

## 配置

```json
{
  "env": {
    "POSTGRES_DB_URL": "postgresql://root:123456@localhost:5432/mydb"
  }
}
```
